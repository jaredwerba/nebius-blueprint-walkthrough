[project]
name = "nebius-blueprint-walkthrough"
version = "0.1.0"
description = "Personal walkthrough of Nebius Agents Blueprint recipes 01-10."
requires-python = ">=3.12"
dependencies = [
    "httpx>=0.27",
]

[dependency-groups]
dev = [
    "pytest>=8.3",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
